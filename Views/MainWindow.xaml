<Window x:Class="AlgorithmPerformanceEvaluator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="Algorithm Performance Evaluator" Height="750" Width="1200"
        Background="#121212" WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.5*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <!-- LEFT PANEL: Code Editor -->
        <Border Grid.Column="0" Background="#1E1E1E" CornerRadius="15" Margin="10">
            <StackPanel Margin="15">
                <TextBlock Text="C# CODE EDITOR" Foreground="#deff9a" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>

                <Border Background="#121212" CornerRadius="10" Padding="5">
                    <TextBox x:Name="txtCodeEditor" 
                     Height="400" 
                     Background="Transparent" 
                     Foreground="#D4D4D4" 
                     CaretBrush="#deff9a"
                     AcceptsReturn="True" 
                     FontFamily="Consolas" 
                     FontSize="14"
                     Padding="10"
                     BorderThickness="0">
                    </TextBox>
                </Border>

                <!-- Manual Input Panel -->
                <StackPanel x:Name="manualInputPanel" Visibility="Collapsed" Margin="0,10,0,0">
                    <TextBlock Text="MANUAL ARRAY INPUT (Comma separated)" Foreground="#aaa" FontSize="12" Margin="5,0,0,5"/>
                    <TextBox x:Name="txtArrayInput" 
                             Height="40" Background="#121212" Foreground="White" 
                             BorderBrush="#2A2A2A" Padding="10" VerticalContentAlignment="Center"
                             Text="10, 5, 8, 1, 7" 
                             CaretBrush="#deff9a"/>
                </StackPanel>

                <!-- MODE BUTTONS -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15">
                    <Button x:Name="btnManual" Content="Manual Mode" Width="110" Margin="5" Background="#2A2A2A" Foreground="White" Click="btnManual_Click"/>
                    <Button x:Name="btnAuto" Content="Auto Mode" Width="110" Margin="5" Background="#2A2A2A" Foreground="White" Click="btnAuto_Click"/>
                </StackPanel>

                <!-- RUN BUTTON -->
                <Button x:Name="btnRun" Content="Run Performance Analysis" Height="50" Background="#deff9a" Foreground="#000" FontWeight="Bold" FontSize="14" Click="btnRun_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="10"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>

        <!-- RIGHT PANEL: Results & Chart -->
        <StackPanel Grid.Column="1" Margin="10">
            <!-- COMPLEXITY INFO -->
            <Border Background="#1E1E1E" CornerRadius="15" Padding="20" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="DETECTED COMPLEXITY" Foreground="#daffde" FontSize="14" Opacity="0.7"/>
                    <TextBlock x:Name="lblComplexity" Text="O(?)" Foreground="#deff9a" FontSize="48" FontWeight="Bold"/>
                    <TextBlock x:Name="lblConfidence" Text="Waiting for analysis..." Foreground="#aaa" FontSize="16" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <!-- CHART AREA -->
            <Border Background="#1E1E1E" CornerRadius="15" Height="430" Padding="15">
                <lvc:CartesianChart x:Name="MyChart" LegendLocation="Bottom">
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Title="Input Size (N)" Foreground="Gray"/>
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis Title="Time (ms)" Foreground="Gray"/>
                    </lvc:CartesianChart.AxisY>
                </lvc:CartesianChart>
            </Border>
        </StackPanel>
    </Grid>
</Window>